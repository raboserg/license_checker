cmake_minimum_required (VERSION 3.8)
project ("constants")
set(SOURCE_LIST constants.cpp constants.h)
add_library(${PROJECT_NAME} STATIC ${SOURCE_LIST})
target_include_directories(${PROJECT_NAME} PRIVATE ${CPPREST_INCLUDE_DIR})
